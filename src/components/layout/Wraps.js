// Wraps.js
import React from 'react';
import '../../styles/layout/Wraps.css';
import CardList from '../pieces/CardList';

const Wraps = () => {
  return (
    <section id="Wraps">
      Wraps
      <hr />
      <CardList props={'Wraps'} />
      <br />
    </section>
  )
}

export default Wraps;