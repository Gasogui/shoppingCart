// Wraps.js
import React from 'react';
import '../../styles/layout/Wraps.css';

const Wraps = () => {
  return (
    <section id="Wraps">
      Wraps
      <hr />
    </section>
  )
}

export default Wraps;