// Ensaladas.js
import React from 'react';
import '../../styles/layout/Ensaladas.css';

const Ensaladas = () => {
  return (
    <section id="Ensaladas">
      Ensaladas
      <hr />
    </section>
  )
}

export default Ensaladas;